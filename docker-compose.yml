version: '3.9'

services:
  backend-despachos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-despachos
    ports:
      - "8082:8080"
    environment:
      - JAVA_OPTS=-Xmx512m -Xms256m -XX:+UseG1GC
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_APPLICATION_NAME=backend-despachos
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - despacho-network
    labels:
      - "com.despacho.description=Backend Despachos - Spring Boot REST API"
      - "com.despacho.version=1.0"
    volumes:
      - ./logs:/app/logs

networks:
  despacho-network:
    driver: bridge
